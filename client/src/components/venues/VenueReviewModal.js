import React from "react";
import Modal from "../general/Modal";

const VenueReviewModal = ({ ...modalProps }) => {
  return <Modal {...modalProps}>This is the venue review modal</Modal>;
};

export default VenueReviewModal;
